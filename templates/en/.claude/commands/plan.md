Read `AGENTS.md`, `docs/index.md`, and the relevant standards, architecture notes, plans, or runbooks first.

Then produce a plan with:
- goal
- constraints
- files likely involved
- implementation approach
- risks
- validation steps
- done condition

Use `docs/plans/active/` when the task is non-trivial enough to need durable execution context.

Do not implement yet.

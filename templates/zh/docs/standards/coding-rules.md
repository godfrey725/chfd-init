# 编码规则

## 通用
- 与仓库现有风格和结构保持一致。
- 做聚焦改动，而不是顺手清理无关内容。
- 优先选择直接、清晰的代码，而不是过早抽象。
- 当你确信某些死代码未被使用时，可以删除它。

## 边界
- 保持文件和模块边界上的职责清晰。
- 不要在未记录的情况下引入跨层依赖。
- 在创建新 helper 前，优先复用项目中已有的基础原语。
- 当边界被反复错误穿越时，应通过文档或约束机制固化规则，而不是依赖记忆。

## 变更卫生
- 同步更新名称、导入和相关引用。
- 避免悄无声息地改变行为。
- 让被修改区域比之前更容易阅读。
- 优先做容易审查、回退和验证的变更。

先阅读 `AGENTS.md`、`docs/index.md`，以及相关的 standards、architecture notes、plans 或 runbooks。

然后给出一个计划，包含：
- goal
- constraints
- files likely involved
- implementation approach
- risks
- validation steps
- done condition

当任务足够非简单、需要可持续执行上下文时，使用 `docs/plans/active/`。

先不要实现。
